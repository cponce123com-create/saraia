# ─── Frontend ──────────────────────────────────────────────
# Ya no se necesita API key en el frontend.
# El proxy serverless (/api/ocr) usa la variable del servidor.
# Para desarrollo local con proxy, configura DEEPSEEK_API_KEY en el servidor.

# ─── Backend / Serverless (Vercel, Netlify, Render) ───────
# DEEPSEEK_API_KEY=sk-tu_api_key_aqui
# ⚠️ Esta variable debe configurarse en el panel del servidor,
#    NO en este archivo. Solo se incluye como referencia.
